# 📅 หน้ากำหนดบำรุงรักษา (Maintenance Schedule)

## 🎯 ภาพรวม

หน้ากำหนดบำรุงรักษาแสดงรายการโครงการที่**ยังไม่เสร็จสมบูรณ์** (สถานะ < 16/16) เพื่อติดตามความคืบหน้าและกำหนดการบำรุงรักษา

## ✨ คุณสมบัติหลัก

### 1. **แสดงเฉพาะโครงการที่ไม่เสร็จ**
   - โครงการที่เสร็จครบ 16/16 จะ**ไม่แสดง**ในรายการ
   - ช่วยให้ admin มองเห็นโครงการที่ต้องติดตามได้ชัดเจน

### 2. **เรียงลำดับตามสถานะและวันที่**
   - เรียงจาก: ลำดับเสาะและวันที่
   - โครงการที่มีความคืบหน้าน้อยจะแสดงก่อน

### 3. **สถิติแบบแยกช่วงขั้นตอน**
   - **ระยะแรก (1-7):** โครงการที่อยู่ในช่วงเริ่มต้น
   - **ระยะกลาง (8-13):** โครงการที่อยู่ในช่วงกลาง
   - **ใกล้เสร็จ (14-15):** โครงการที่ใกล้เสร็จสมบูรณ์

### 4. **ตารางแสดงข้อมูล**
   - ลำดับ
   - วิคควบรยาน (ชื่อหน่วยงาน)
   - จังหวัด
   - ผู้รับเหมา (EPC)
   - ผู้ประสานงาน
   - ครบกำหนด (วันที่ส่งเหมา)
   - **สถานะ (X/16)** - แสดงจำนวนขั้นตอนที่เสร็จแล้ว

## 🎨 UI Design

### Color Coding สำหรับสถานะ
```javascript
0-7 ขั้นตอน   → 🔴 สีแดง (ระยะแรก)
8-13 ขั้นตอน  → 🟡 สีเหลือง (ระยะกลาง)
14-15 ขั้นตอน → 🟢 สีเขียว (ใกล้เสร็จ)
16 ขั้นตอน    → ❌ ไม่แสดง (เสร็จสมบูรณ์)
```

### ตาราง
- **Header:** สีส้ม (#f97316)
- **แถว:** สลับสีส้มอ่อน/ขาว
- **Hover:** เปลี่ยนเป็นสีส้มอ่อนเมื่อ hover

## 📊 ตัวอย่าง UI

```
┌─────┬────────────────────────────┬───────────┬──────────────────┬──────────────┬─────────────┬─────────┐
│ ลำดับ│ วิคควบรยาน                 │ จังหวัด    │ ผู้รับเหมา (EPC) │ ผู้ประสานงาน │ ครบกำหนด   │ สถานะ   │
├─────┼────────────────────────────┼───────────┼──────────────────┼──────────────┼─────────────┼─────────┤
│  1  │ โรงเรียนตระหนักงคดัติทยาคม │ ยโสธร     │ บริษัท เอ็น... │ นางสาวภูศิริ์│ 7/2568      │ 13/16   │
│  2  │ โรงเรียนกี่งวิทยา          │ ยโสธร     │ บริษัท เอ็น... │ นางสาวภูศิริ์│ 8/2568      │ 13/16   │
│  3  │ โรงเรียนป่งน้อยทียาคม     │ ชุมสรซาชานี│ บริษัท เอ็น... │ นางสาวภูศิริ์│ 9/2568      │ 14/16   │
│  4  │ โรงเรียนเมืองในทีชยาชาธ   │ ชุมสรซาชานี│ บริษัท เอ็น... │ นางสาวภูศิริ์│ 2/2569      │ 15/16   │
└─────┴────────────────────────────┴───────────┴──────────────────┴──────────────┴─────────────┴─────────┘

                        *ค่าควบ 16/16 จะหายไปจากหน้านี้*
```

## 🔍 ฟีเจอร์การค้นหาและกรอง

### 1. **Search Box**
   - ค้นหาด้วย: ชื่อหน่วยงาน หรือจังหวัด
   - Real-time search

### 2. **Filter ตามช่วงขั้นตอน**
   - ทั้งหมด
   - ระยะแรก (1-7)
   - ระยะกลาง (8-13)
   - ใกล้เสร็จ (14-15)

### 3. **Pagination**
   - แสดงผล 10 รายการต่อหน้า
   - มีปุ่มเลขหน้าและ prev/next

## 📝 การทำงาน

### 1. Load Data
```javascript
// ดึงข้อมูลโครงการทั้งหมด
const allProjects = await projectsAPI.getProjects({ limit: 100 });

// ดึงข้อมูลขั้นตอนของแต่ละโครงการ
const projectsWithSteps = await Promise.all(
  allProjects.map(async (project) => {
    const steps = await projectsAPI.getProjectSteps(project.id);
    const completedSteps = steps.filter(s => s.status === 'completed').length;
    return {
      ...project,
      completedSteps,
      totalSteps: 16,
      stepStatus: `${completedSteps}/16`
    };
  })
);

// กรองเฉพาะที่ไม่เสร็จ (< 16)
const incompleteProjects = projectsWithSteps.filter(p => p.completedSteps < 16);
```

### 2. Color Logic
```javascript
const getStatusBadgeColor = (stepStatus) => {
  const [completed, total] = stepStatus.split('/').map(Number);
  if (completed < 8) return 'bg-red-100 text-red-700';
  if (completed < 14) return 'bg-yellow-100 text-yellow-700';
  return 'bg-green-100 text-green-700';
};
```

### 3. Filter Logic
```javascript
if (filterStatus === 'early') matchesFilter = completed < 8;
else if (filterStatus === 'mid') matchesFilter = completed >= 8 && completed < 14;
else if (filterStatus === 'late') matchesFilter = completed >= 14;
```

## 🚀 การใช้งาน

### สำหรับ Admin:

1. **เข้าสู่หน้ากำหนดบำรุงรักษา:**
   ```
   https://devwooyou.space/admin → เมนูสถานะ → กำหนดบำรุงรักษา
   ```

2. **ดูสถิติรวม:**
   - ดูจำนวนโครงการทั้งหมดที่ยังไม่เสร็จ
   - แยกตามช่วงขั้นตอน (ระยะแรก/กลาง/ใกล้เสร็จ)

3. **ค้นหาโครงการ:**
   - พิมพ์ชื่อหน่วยงานหรือจังหวัดใน search box

4. **กรองโครงการ:**
   - เลือก filter ตามช่วงขั้นตอนที่ต้องการดู

5. **เข้าดูรายละเอียด:**
   - คลิกที่โครงการเพื่อดู 16 ขั้นตอนโดยละเอียด (ใน "อัพเดทสถานะ")

## ⚠️ หมายเหตุสำคัญ

### 1. **โครงการที่ 16/16 จะหายไป**
   - โครงการที่เสร็จครบทั้ง 16 ขั้นตอนจะ**ไม่แสดง**ในหน้านี้
   - เพื่อให้มองเห็นเฉพาะโครงการที่ยังต้องติดตาม

### 2. **เรียงลำดับ**
   - *เรียงจาก ลำดับเสาะและวันที่*
   - โครงการที่มีสถานะน้อยกว่าจะแสดงก่อน

### 3. **Performance**
   - หากมีโครงการจำนวนมาก การโหลดครั้งแรกอาจใช้เวลาสักครู่
   - ระบบจะดึงข้อมูล steps ของทุกโครงการ

## 🔧 API ที่ใช้

### 1. Get All Projects
```http
GET /api/projects?limit=100
Authorization: Bearer {token}
```

### 2. Get Project Steps
```http
GET /api/project-steps/:projectId
Authorization: Bearer {token}
```

### 3. Get EPC Companies
```http
GET /api/epc/list
Authorization: Bearer {token}
```

### 4. Get Coordinators
```http
GET /api/coordinators?limit=100
Authorization: Bearer {token}
```

## 📁 ไฟล์ที่เกี่ยวข้อง

### Frontend:
- **`src/components/MaintenanceSchedule.jsx`**
  - Component หลักของหน้ากำหนดบำรุงรักษา
  - ดึงข้อมูล, กรอง, แสดงผล

### Backend:
- **`backend/routes/projects.js`**
  - API สำหรับดึงข้อมูลโครงการ
  
- **`backend/routes/project-steps.js`**
  - API สำหรับดึงข้อมูล 16 ขั้นตอน

## 🐛 Troubleshooting

### ปัญหา: ไม่แสดงข้อมูลโครงการ

**สาเหตุ:**
1. ยังไม่มีโครงการในระบบ
2. โครงการทั้งหมดเสร็จครบ 16/16 แล้ว
3. API error

**วิธีแก้:**
```bash
# ตรวจสอบ logs
pm2 logs evolutions-backend --lines 50

# ตรวจสอบ database
mysql -h 145.223.21.117 -u debian-sys-maint -p
SELECT COUNT(*) FROM project_registrations;
```

### ปัญหา: Loading ช้า

**สาเหตุ:**
- ดึงข้อมูล steps ของทุกโครงการทีละรายการ

**วิธีแก้:**
- เพิ่ม caching
- ใช้ batch query
- เพิ่ม loading indicator

### ปัญหา: สถานะไม่ถูกต้อง

**สาเหตุ:**
- ข้อมูล steps ใน database ไม่ update

**วิธีแก้:**
```bash
# ตรวจสอบ project_steps table
mysql -h 145.223.21.117 -u debian-sys-maint -p
USE eep_management;
SELECT projectId, COUNT(*) as total, 
       SUM(CASE WHEN status='completed' THEN 1 ELSE 0 END) as completed
FROM project_steps 
GROUP BY projectId;
```

## 🆚 ความแตกต่างจาก "อัพเดทสถานะ"

| Feature | อัพเดทสถานะ | กำหนดบำรุงรักษา |
|---------|------------|-----------------|
| แสดงโครงการ | ทั้งหมด | เฉพาะที่ไม่เสร็จ (< 16/16) |
| จุดประสงค์ | อัพเดทสถานะ + ดูรายละเอียด 16 ขั้นตอน | ติดตามความคืบหน้า |
| สถานะที่แสดง | X/18 (mock) | X/16 (จริง) |
| Filter | ทั่วไป | แบ่งตามช่วงขั้นตอน |
| สีตาราง | สีส้ม | สีส้ม |

## 📞 Support

หากพบปัญหา:
```bash
# ตรวจสอบ logs
pm2 logs evolutions-frontend --lines 50
pm2 logs evolutions-backend --lines 50

# Restart services
pm2 restart evolutions-frontend evolutions-backend
```

---

**เวอร์ชัน:** 1.0.0  
**อัพเดทล่าสุด:** 3 พฤศจิกายน 2025  
**ผู้พัฒนา:** EEP Management System Team


